using UnityEngine;
using System.Collections;
using UnityEngine.PostProcessing;

public class RegisterPostBehaviourToSettingsManager : MonoBehaviour
{
    private PostProcessingBehaviour behaviour;

	// Use this for initialization
	void Start ()
	{
	   // behaviour = GetComponent<PostProcessingBehaviour>();
       // SettingsManager.instance.RegisterPostProfile(behaviour.profile);
	}
	
}
